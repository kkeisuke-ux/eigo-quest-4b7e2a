# えいごクエスト PROGRESS

## 2026-08-11 第16回フィードバック（A/F/G/M/N/R/U発音の追加修正）

**症状**: 第15回でD/G/Zは直ったが、A/F/G/M/N/R/Uがまだ不明瞭（特にAが「アイ」に聞こえる）。

- **原因**: 第15回で採用した「文字の呼び名の英単語スペル」（"ay"/"eff"/"em"等）方式が
  一部の文字で裏目に出ていた。特にA="ay"は、Piperが単独の"ay"を間投詞"aye"（＝はい、
  発音は/aɪ/＝「アイ」）として読んでしまい、文字Aの本来の音/eɪ/にならなかった。
  R="ar"も単独では語尾の/r/（rhotic）が弱まり「あー」に近くなっていた
- **修正**: 複数の綴り候補（単語スペル／大文字+ピリオド／大文字のみ等）をamy・lessac
  両音声で生成しWhisperで比較した結果、**「大文字1文字+ピリオド」**（例:"A."）が最も
  安定して文字名として読まれることが判明。この方式に26字中23字を統一（Whisper転写一致：
  旧方式20字→新方式22字に改善、かつ問題報告のあった全字が解消）。
  Q（"cue"）・T（"tee"）は元のスペルの方が良かったため維持。Z（"zee"・lessac）は
  第15回の修正のまま維持
- 検証: Whisper（small.en, VAD有効）で26字を再転写。A/F/G/M/N/R/Uすべて期待通りの
  文字/文字名で認識されることを確認

## 2026-08-10 第15回フィードバック（D/G/Z発音・にがてなもじだけ・バッジ重複）

**症状**: ①アルファベットテストを途中で抜けると最初からになる ②D/Z/Gの発音が聞き取りにくい
③テストで間違えた時も書き直し前に発音してほしい ④なかま画面でコイン/スターが2重表示
⑤アルファベットは間違えた字だけ復習・テストしたい（毎回26字は大変）

- ①③はコード上は前回セッション（未コミットのまま残っていた第10〜14回の作業）で対応済み
  だったため、Playwrightで実機相当の動作を検証: 「とちゅうから」で正しい問題数から再開／
  間違えた瞬間に効果音+文字名の再発音が鳴ることを確認（コード変更なし・検証のみ）
- **②D/Z/G発音の根本修正**: 単独の大文字1文字をそのままPiperに渡していたため文字名として
  安定して読まれていなかった。26字すべてを「文字の呼び名」のスペル（dee/gee/zee等）に
  変更して再生成。Zだけは`zee`という綴りにしても amy 音声だと語頭の /z/ が /s/ に無声化
  してしまい直らなかったため、その1字だけ lessac 音声（F0実測 amy≈208Hz / lessac≈191Hz
  で近い声質）に差し替え。Whisper（small.en, VAD有効）で全26字を転写検証しD/G/Zとも
  正しく認識されることを確認（26字中20字が転写一致。残り6字は"eff""ar"等そもそも短い
  非単語のためASRが苦手なだけで、実際の聞き取りやすさとは別問題と判断し未対応）
- **④コイン/スター重複表示を修正**: TopBarが常にStatusChips（コイン・スター・バディ）を
  出すのに、なかま画面・ガチャ画面が独自にCoinBadge/StarBadgeをrightへ渡していたのが原因。
  重複分を削除
- **⑤「にがてなもじだけ」モードを新設**: アルファベットハブ画面に、テストで「こたえを見る」
  を使った（＝間違えて、まだ習得しなおしていない）字だけを抽出して練習・テストできるボタンを
  追加。テスト結果画面にも同様のショートカットボタンを設置。練習だけした字（間違い扱いなし）は
  対象に含めない。既存の「全26字」練習・テストはそのまま残し追加の選択肢として提供
- 検証: Playwright実ブラウザでテスト再開・自動再発音・にがてなもじ抽出（1字を間違えた
  直後にハブへ「にがてな1もじだけ」ボタンが出現）を確認。`npx tsc --noEmit`・`npm run build`成功

## 2026-08-09 第14回フィードバック（アルファベット練習の自動発音を全ラウンドに）

- アルファベット練習（ガイド→なぞる→自分で書く）で、自動発音が最初のラウンドしか鳴らなかった → **ラウンドが変わるたび・まちがえて書き直すたびに毎回発音**するように修正
- 原因: useAutoSpeakのkeyが文字indexのみだった → `${index}-${round}-${tries}` に変更（1行修正）

## 2026-08-09 第13回フィードバック（絵日記改善・到達レベル表示・ひとりぶんひっこし）

1. **絵日記一覧のサムネに手書き英文も表示**（絵の下に破線区切りで、書かれた範囲に合わせた高さで描画）。日付は**年入り**（2026年8月8日（ど））に
2. **編集画面の並び替え**: 絵 → 英文を書くところ → れいぶん検索（文字書きが絵のすぐ下に）
3. **書き味改善**: ①2Dコンテキストをキャッシュ（pointermoveごとのgetContext廃止）②絵・文字カードに `touch-action: none`（手のひらがカードに触れてもページがスクロールしない）
4. **到達レベル表示**（かんじクエストと同じ仕組み）: まとめテスト100点の最高到達を「Lv ようじ 1学期」形式で、起動画面のプロフィールカード（学年の代わり）・ホームの名前横・みんな画面に表示。words.tsに `termClearLevelLabel` 新設
5. **ひとりぶんひっこし**（かんじクエストv1.9.0の移植）: 設定に「○○だけ AirDrop/ファイルに書き出す」。読み込みは共通ボタンでファイル種別を自動判別し、その人だけ追加・上書き（ownedCharacters idの振り直し+buddyId追随）。全員分AirDrop共有ボタンも追加
- 検証: 実ブラウザで バッジ3画面表示・年入り日付・文字サムネ（img2枚）・カード順（絵→文字→検索）・ひとりぶん読み込み往復（日記/テスト記録の維持・重複なし）を確認

## 2026-08-09 第12回フィードバック（発音音声をアプリに内蔵＝TTS依存の根絶）

**症状**: まだ変な声で、声えらびの一覧にもまともな声がない → この端末のブラウザにはまともな英語TTSが公開されていないと判明。TTS依存を根本的にやめた。

- **Piper TTS**（完全ローカル・MITライセンス・en_US-amy-medium=クリアな女性声）で**アルファベット26字・全600単語・全例文=715件のmp3を事前生成**（計3.9MB）し、public/audio/voice/ に内蔵
- 再生はWeb Audio（voiceBus）経由: 音量スライダー反映・増幅1.3倍（TTSより大きく鳴らせる）・BGM ducking対応・iOSの音量無視問題なし
- speak()の共通入口で内蔵音声を最優先。**単語・文字・例文はどの端末でも同じクリアな女性声**。内蔵にない文（えにっきの自由文など）だけTTSへフォールバック（第11回の声えらびはその用途に残置）
- 再生成手順: `node scripts/gen-voice.mjs`（ツール: `../_tools/piper/`。piper.exeは日本語パスでクラッシュするためASCIIパスへ自動コピーして実行）
- 検証: Whisper（base.en）で生成音声を転写チェック（a/b/h/w/apple/dog/water/birthday/例文2件=正転写。zは単独文字ゆえ転写曖昧だが「zee」綴りで生成し人間には明瞭）。ブラウザで「ためす」→apple.mp3再生・TTS不使用を確認
- 語頭/z/の子音がやや弱い癖あり（自然発話でも起こる部分無声化の範囲。気になれば別モデルlessacへの差し替え可）

## 2026-08-09 第11回フィードバック（笑っているような変な声の根治・声を選べる機能）

**症状**: 音量は改善したが発音が「笑っているような変な声」。設定の試し聞きも変。

- **原因**: iOSはロボット声ファミリー「Eloquence」（Jester=笑い声・Flo・Eddy等）を持ち、さらに**端末が日本語設定だと音声の表示名がローカライズされる**ため、英語名ベースのブラックリスト・優先リストが一致していなかった
- **修正1**: 判定を表示名に加えて**voiceURI**（com.apple.eloquence.… 等、ローカライズされない）でも行う。`eloquence` を含むURIを全て除外（Jester等を一網打尽）。優先リストもURI照合になり「サマンサ（拡張）」のような日本語表示でも Samantha を確実に選べる
- **修正2**: 優先順を**クリアな女性声**に変更（Samantha/Ava/Allison/Zoe/Susan/Vicki → Google US English → 英語圏女性 Karen/Moira/Tessa/Serena/Kate → 男性標準声）
- **修正3（保険）**: 設定画面に**「えいごの声を えらぶ」**を新設。端末の英語音声を「きれいな声→※へんな声」の順で一覧し、選ぶと即試し聞き。選択はvoiceUriとして保存され最優先で使われる（自動選択がその端末で外れても手動で確実に直せる）
- 検証: iOS風のモック音声リスト（日本語名アルバート・Jester(default)・eloquence Flo・compact/premium Samantha等）で、自動選択=サマンサ（拡張）・ジョーク声の分類・一覧の並びを確認

## 2026-08-09 第10回フィードバック（音声の全面改善）

**症状**: ①発音の音量が小さすぎる ②iPhoneのChromeで発音が潰れた変な男性声 ③BGMが設定最小でも大きすぎる

- **③の原因（最重要）**: bgm-home.mp3等の音源ファイルが配置済みで、iOS系ブラウザ（iPhone Chrome含む・全てWebKit）は `HTMLAudioElement.volume` を無視するため、**ファイルBGMがフル音量で再生され音量スライダーもduckingも効いていなかった**。①はこのBGMに発音（TTS=最大でも小さめ）が埋もれていたのが主因
- **修正**: ファイルBGMを `createMediaElementSource` → 専用GainNode（fileBgmBus）→ destination のWeb Audio経路に変更（EQ/リバーブは通さず素の音）。SEファイルも `decodeAudioData` でバッファ化して seBus 経由に。これで**iOSでも音量スライダーと発音中ducking（0.12→0.05に強化）が効く**
- **②の修正**: 音声選択にAppleのジョーク系音声（Albert・Fredなど約28種）のブラックリストを追加。優先リストにAaron・Google US Englishを追加し、端末既定音声→ローカル音声の順で選択。voiceschangedを継続リッスンし高品質音声が遅れて届いたら乗り換え
- **バランス**: 効果音の既定音量 0.5→0.35（旧既定0.5の保存値は自動移行。手動変更値は尊重）
- 検証: PC ChromeでBGMがMediaElementSource経由（el.volume=1固定・バス制御）で再生されることをAPIフックで確認。ビルド成功・デプロイ済み
- 今後の選択肢: 発音のさらなる音質向上には録音音声ファイル（public/audio/voice/）の配置が有効（AudioProvider構造は対応済み）

## 現在の状態（2026-08-08）

**MVP完成・ビルド成功・主要フロー動作確認済み。実機（iPad + Apple Pencil）検証が次の一手。**

- 仕様 §67 の完成条件 A〜O をすべて実装（詳細は README §2）
- `npm run build` 成功、Service Worker生成済み、PWAとしてオフライン動作可
- Playwright（PC Chrome）で動作確認済み:
  - プロフィール作成 → ホーム → アルファベットなぞり（書き順ガイド・始点●・方向アニメ表示）
  - 単語4ステップ練習: 合成ポインタで「dog」を筆記 → 自動判定 → 正解○ → STEP2へ進行
  - 絵日記: 添削「i go park yesterday」→「I went to the park yesterday.」＋説明4件（仕様§30の例どおり）
- 認識エンジンの自動テスト: 自己分類 52/52、ノイズ・回転・書き順逆転 156/156、
  別文字の誤受理 0/120、約85ms/判定（README §3）

## 次の一手

1. **実機検証**: `npm run deploy`（GitHub Pages: リポジトリ `eigo-quest-4b7e2a`）→ iPadのSafariで開き
   「ホーム画面に追加」→ Apple Pencilで書いて、設定→Apple Pencil診断で palm rejection を確認
2. 子どもの実筆記で判定のきびしさを調整（設定 → 判定デバッグ。サンプル収集・ラベル付け機能あり）
3. 音源ファイルの配置（README §4 の対応表。OtoLogic / DOVA-SYNDROME からダウンロードして
   `public/audio/` へ。無くても合成音で全機能動作する）

## 残タスク（MVP外・優先度低）

- 英文練習モード（sentences.ts のお手本書き写し画面。データは準備済み）
- 単語イラストの本格画像化（現在は絵文字。構造は差し替え可能）
- 語彙の拡張（現在120語: ようじ×3学期＋小1相当×3学期。words.jsonに追記するだけ）
- キャラクター追加（現在10系列25段階。species.tsに追記するだけ）

## 技術メモ

- かんじクエストがポート4173/4174等を使っていることがあるため、previewは空きポートに自動移動する
  （起動ログに実際のURLが出る）
- PC開発時はマウスでも書ける（pointerType 'mouse' を許可。'touch' は既定で拒否 = palm rejection）
- 旧Service Workerが残っている場合は DevTools → Application → Service Workers から削除
