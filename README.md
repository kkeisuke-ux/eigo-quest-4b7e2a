# えいごクエスト（eigo-quest）

iPad + Apple Pencil + Safari で使う、家族5人用の英語手書き学習PWA。
アルファベット（A〜Z / a〜z）→ 英単語 → 短い英文 → 「えいご絵日記」へと発展する。
学習の中心は **英単語を「聞く・見る・イメージと結びつける・実際に何度も書く」** こと。

かんじクエスト（`202608_kanji-quest`）の実証済み基盤（Apple Pencil入力・掌拒否・IndexedDB・
ゲームシステム・PWA）を土台に、英語版として新規開発した。**データ・ゲーム内容は完全に独立**
（DB名 `eigo-quest`、キャラクター・世界観も別物）。

---

## 1. 使い方（開発）

```bash
npm install
npm run dev        # 開発サーバー（PCで確認。マウスでも書ける）
npm run build      # 本番ビルド（tsc + vite + Service Worker生成）
npm run preview    # dist/ をローカル配信（ポート4174。使用中なら自動で次へ）
npm run gen:icons  # PWAアイコン再生成
npm run deploy     # GitHub Pages へ公開（リポジトリ eigo-quest-4b7e2a）
```

iPadでの利用: 公開URLをSafariで開き「ホーム画面に追加」→ フルスクリーンのPWAとして起動。
2回目以降はオフラインでも動作する（Service Workerが全アセットをキャッシュ）。

## 2. 実装済み機能（仕様 §67 完成条件との対応）

| 条件 | 内容 | 状態 |
|---|---|---|
| A | Apple Pencilで大文字・小文字のなぞり練習（書き順ガイド・始点●・方向アニメ） | ✅ |
| B | アルファベット表示と同時に自動発音（アメリカ英語） | ✅ |
| C | 英単語5語 = 発音＋イラスト＋筆記4回（なぞり×2 → 見て書く → 思い出して書く） | ✅ |
| D | 5問テスト（日本語 → 英単語筆記。答えは読み上げない） | ✅ |
| E | 正解時ピンポーン＋大きな○アニメ | ✅ |
| F | 不正解時は柔らかいSEで再挑戦（2回目から「お手本を見る」） | ✅ |
| G/H | 文字の判定 やさしい／ふつう／きびしい（初期値: やさしい） | ✅ |
| I | 5人プロフィール個別保存 | ✅ |
| J | コイン・ガチャ・スター購入 | ✅ |
| K/L/M | スター育成・レベルアップ・進化・図鑑 | ✅ |
| N | えいご絵日記（Apple Pencilの絵＋英文手書き）保存・一覧・見返し | ✅ |
| O | 絵日記1日分のPDF出力（添削文を含める/含めないを選択可） | ✅ |

追加実装: 復習（間隔反復＋「音を聞いて書く」問題）・わからない単語リスト・
まとめテスト（範囲全語連続・途中保存・最高点）・みんな画面（横並び比較＋最近のできごと）・
ことばを調べる／わたしの単語帳・Apple Pencil診断・判定デバッグ・バックアップ書き出し/読み込み。

## 3. 認識エンジン

- 1文字 = 1ボックスに書き、テンプレートマッチング（DTW＋フレシェ＋Hungarian）で判定
- **box系**（4線ガイド上の位置・大きさ）と **shape系**（純粋な字形）の混合コスト
  → 大きさの帯で c/C などを区別しつつ、多少のズレ・傾きは許容
- 書き順・画の向きは不問（順序フリーのマッチング。仕様 §8, §14）
- O のような閉曲線は書き始めの位置もフリー（回転不変マッチング）
- 大文字と同形の小文字（c k o s u v w x z）は同じ文字として扱う
- しきい値は 設定 →「判定デバッグ」で実機調整・保存できる

ビルド版での自動テスト結果（`window.__eigoDev` 経由）:

| テスト | 結果 |
|---|---|
| 参照データ自身の分類（52文字） | 52/52 |
| ノイズ＋移動＋縮小拡大＋書き順逆転（156ケース） | 156/156 |
| 別の文字を書いたときの誤受理（120ケース） | 0件（全拒否） |
| 判定速度 | 約85ms/文字 |

## 4. 音声・音源

### 英語発音
- ブラウザ内蔵TTS（SpeechSynthesis）の **en-US音声をfeature detection**して使用
  （iPadではSamantha等の高品質音声を優先選択）
- 単語・アルファベットカードの表示と同時に自動発音、ボタンで何度でも聞き直せる
- 発音中はBGMが自動で下がり、終了後0.4秒かけて戻る（audio ducking。仕様 §35）
- `src/audio/tts.ts` の `PronunciationProvider` に実装を追加すれば、
  将来 `public/audio/voice/` の録音ファイルへ差し替えられる構造

### BGM・効果音（仕様指定の音源）
著作権上、音源ファイルはリポジトリに**同梱していない**。以下の対応表のファイル名で
`public/audio/` に置くと自動的にそれが再生され、**無い間はアプリ内蔵の合成音**（Web Audio API）で鳴る。

| ファイル名 | 指定音源 |
|---|---|
| `bgm-home.mp3` | DOVA-SYNDROME「ポップン・ダッシュ」（ホーム・なかま画面） |
| `bgm-study.mp3` | DOVA-SYNDROME「おでかけしましょ」（学習・テスト画面。小音量） |
| `se-correct.mp3` | OtoLogic「クイズ ピンポン04-1（短）」（正解） |
| `se-finish.mp3` | OtoLogic「GB 汎用 B07-1（クリア1）」（テスト完走） |
| `se-perfect.mp3` | OtoLogic「GB RPG B14-4（勝利・ショート）」（全問正解） |
| `se-gacha-start.mp3` | OtoLogic「場面展開12-1（長・ディレイ）」（ガチャ・進化開始） |
| `se-gacha-new.mp3` | OtoLogic「マルチアクセント12-1（中）」（新しい仲間） |
| `se-evolve.mp3` | OtoLogic「ベルアクセント14-1（高）」（進化完了） |
| `se-wrong.mp3` ほか | 柔らかい再挑戦音など（OtoLogic等から統一感のあるものを選ぶ） |

その他の対応ファイル名は `src/audio/sound.ts` の `SE_FILES` を参照。
利用時は各サイトの利用規約を確認のこと（OtoLogic: CC BY 4.0 でクレジット表記、
DOVA-SYNDROME: 利用規約に従う）。アプリ内の「せってい → つかっている音の素材」にも表記済み。

## 5. データの拡張方法（データ駆動設計）

| 追加したいもの | 触るファイル |
|---|---|
| 英単語・ステージ・学年レベル | `src/data/words.json`（語彙は数千語まで拡張可能な構造） |
| 単語イラスト | `words.json` の `emoji`。画像に差し替える場合は `illustration.kind: 'image'` 対応をローダーに追加 |
| アルファベット書き順 | `src/data/alphabet.ts`（SVGパス、viewBox 100、4線 = 14/42/76/94） |
| キャラクター | `src/data/species.ts`（現在10系列・25段階。100種類以上へ拡張可能） |
| 例文（英文練習） | `src/data/sentences.ts` |
| 報酬・確率の調整 | `src/config/gameConfig.ts` |
| 判定しきい値 | `src/config/judgeConfig.ts`（実機では「判定デバッグ」画面から） |

## 6. 設計上の仮定（仕様 §68「合理的な仮定はREADMEへ記録」）

1. **単語筆記は「1文字=1ボックス」方式**。仕様§16の「1文字ずつ認識→結合→比較」を
   確実に実装でき、幼児にも書きやすい（マス数が答えの文字数のヒントになるのは意図的な足場）。
2. **イラストはMVPでは絵文字**（1〜2個の組み合わせ）。「シンプル・明るい・幼すぎない」を
   満たしつつ全120語に即日対応するため。データ構造（`WordIllustration`）は画像差し替え可能。
   曜日は日本語の曜日連想（🔥📅=火曜 など）で覚えやすくした。
3. **アルファベットなぞりは1文字2ラウンド**（ガイド付き→薄字のみ）。テンポ重視（仕様§1）。
4. **書き順は日本の学校で一般的なブロック体の書き方に準拠**（複数の流儀があるため、
   テストでは書き順を問わず「読める字形か」だけを判定。仕様§8どおり）。
5. **絵日記の手書き英文認識は「下読み」**。x座標クラスタリング＋文字分類で推定し、
   本人が読み取り結果を確認・修正してから添削する（自由筆記の完全認識は現実的でないため）。
6. **英文添削はオフラインのルールベース**（スペル距離・文頭大文字・I・ピリオド・be動詞・
   go to the・過去形）。仕様§30の例（i go park yesterday → I went to the park yesterday.）に対応。
   構造は差し替え可能で、将来LLM API等の高精度添削を追加できる。
7. **PDF出力は印刷ダイアログ経由**（外部ライブラリなし・オフライン動作）。iPadのSafariでは
   印刷プレビューをピンチアウト → 共有 → 「"ファイル"に保存」でPDFになる。
8. **絵日記の線の強弱（筆圧）は保存しない**（保存容量対策。色・太さ2種は保存する）。
9. 学年選択に「ようじ」を追加（grade=0）。単語レベルは学年と独立に全員が全レベルを学べる
   （まとめテストのレベル表示は「小1相当」等。公式配当とは表現しない。仕様§25）。
10. 連続学習ボーナス（仕様§40）は `gameConfig.coins.streakBonus` として定義済みだが、
    初期版では未配線（今後 ログイン日判定を追加して有効化する）。

## 7. フォルダ構成（仕様 §64）

```
src/
  core/ink/        Apple Pencil入力（palm rejection・coalesced events・診断）
  core/judge/      ストローク距離（DTW/フレシェ/Hungarian）・なぞり判定
  core/            svgPathパーサ・幾何・お手本データ前処理（refdata）
  recognition/     文字分類・期待文字判定・単語スペル判定
  audio/           TTS発音・BGM/SE（ファイル優先+合成フォールバック）・ducking
  data/            アルファベット書き順・単語JSON・例文・キャラ・学年ラベル
  learn/           なぞり/1文字/単語パッド・単語カード・テストランナー
  screens/         各画面（ホーム/アルファベット/単語/テスト/復習/絵日記/なかま/みんな/設定/診断）
  diary/           手書き英文認識・ルールベース添削・PDF出力・ストローク保存変換
  game/            コイン/ガチャ/スター/レベル/進化ロジック・パラメトリックSVGスプライト
  profiles         screens/ProfileSelect.tsx（5人分離）
  storage/         IndexedDB（16ストア）・リポジトリ層・バックアップ
  state/           軽量ストア（useSyncExternalStore）・非同期フック
  config/          ゲーム設定・判定しきい値・アプリフラグ（音量3系統）
  ui/              共通部品（TopBar/Button/LetterSvg/JudgeMark/セレブレーション等）
```

## 8. 主要な操作フロー

- **アルファベット**: ホーム → アルファベット → 文字タップ（そこから練習）or テスト。
  テストは「文字名を聞いて書く」。読める字形なら正解、正解で習得数が増える（26文字別管理）。
- **単語**: ホーム → たんご → ステージの「れんしゅう」（4ステップ×5語・途中保存）→「5もんテスト」。
  テスト正解で「おぼえたたんご」に加算、わからない/不正解は復習リストへ（テスト正解で自動卒業）。
- **まとめテスト**: ホーム → まとめテスト。学期の全20語連続・1問ごと自動保存・最高点更新。
- **絵日記**: ホーム → えいご絵日記 → きょうのにっき。絵（8色ペン）＋英文手書き →
  「かいた字をよみとる」→ 確認・修正 → 「えいぶんをチェック」→ 保存 / PDF。
- **ゲーム**: 学習でコイン → ガチャ（外れあり・天井3回・初回確定）/ スター購入 → バディ育成 →
  レベルアップ → 進化（次の姿は事前に見せない）→ 図鑑。

## 9. 既知の制限・今後

- 音源ファイル未配置のため、現在は合成音（それでも全SE/BGMが鳴る）
- 絵日記の手書き英文認識は文字が離れて丁寧に書かれている場合に有効（続け字は不可）
- 英文練習モード（お手本例文の書き写し。sentences.ts）は画面未実装（データのみ）
- 連続学習ボーナスの日次判定が未配線
- 実機（iPad + Apple Pencil）でのしきい値調整を推奨: 設定 → 判定デバッグで
  子どもの実筆記にラベルを付けて調整できる

## 10. クレジット

- 効果音（配置時）: OtoLogic (https://otologic.jp) — CC BY 4.0
- BGM（配置時）: DOVA-SYNDROME (https://dova-s.jp)
- 音源未配置時の音: アプリ内蔵の Web Audio API 合成音
- キャラクター・イラスト・UI: 完全オリジナル（パラメトリックSVG＋絵文字）
- フレームワーク: React 19 / TypeScript / Vite 6（依存はこの3系のみ）
